# Claude Code worktrees — these contain checkouts of OTHER repos.
# Never remove this line: without it, unrelated projects get committed here.
.claude/worktrees/

# Corpus data — bulk lives outside the repo, see docs/phase1-recon.md
data/*.sqlite
data/*.sqlite-journal
data/*.csv
data/*.log
data/llm_batch_id.txt
*.pdf

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# OS
Thumbs.db
desktop.ini

# Tesseract language data — fetched by hand, see docs/phase3-design.md §4
data/tessdata/
